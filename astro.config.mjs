import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://divgamingonline.github.io',
  base: '/divgaming-astro-staging',
  output: 'static',
  trailingSlash: 'always'
});
