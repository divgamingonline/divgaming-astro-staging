---
import BaseLayout from '../layouts/BaseLayout.astro';
import buildsData from '../../data/builds.json';
import exoticsData from '../../data/division-2/exotics.json';
import namedItemsData from '../../data/division-2/named-items.json';
import { getBuildVideos } from '../utils/builds';
import { normalizeItems } from '../utils/division2Tools';
import { getCreators } from '../utils/creators';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const videos = getBuildVideos(buildsData, 'all');
const creatorCount = getCreators(buildsData).length;
const toolsCount = 6;
const exoticCount = normalizeItems(exoticsData).length;
const namedCount = normalizeItems(namedItemsData).length;
---

<BaseLayout
  title="DivGaming | The Division 2 Builds, Guides, Tools & Vendor Intel"
  description="Explore Division 2 builds, gear tools, creator videos, guides, and reference databases built for new, returning, and endgame players."
  canonical="/"
  noindex={true}
>
  <header class="production-hero">
    <div class="shd-terminal">
      <div class="terminal-header">
        <span class="terminal-dot"></span>
        <span class="terminal-label">DIVGAMING UPDATE</span>
        <span class="terminal-pulse">STAGING REVIEW</span>
      </div>
      <div class="terminal-content">
        <div class="terminal-prompt">
          <span class="prompt-symbol">&gt;</span>
          <span class="prompt-text">Find builds, gear tools, guides, creator videos, and Division 2 resources faster.</span>
        </div>
      </div>
      <div class="terminal-blink"></div>
    </div>

    <section class="hero shell">
      <div class="hero-copy">
        <img
          class="hero-logo"
          src={`${base}/assets/img/division2-logo-white-2-1086.webp`}
          alt="Tom Clancy's The Division 2"
          width="543"
          height="130"
        />

        <p class="eyebrow">DivGaming // Builds // Tools // Guides</p>
        <h1>Division 2 Player Hub</h1>
        <p class="hero-text">
          DivGaming helps players find the right build, understand gear, open useful
          tools, watch creator videos, and get back into The Division 2 without
          digging through scattered resources.
        </p>

        <div class="hero-actions">
          <a class="btn primary" href="#mission-control">Start here</a>
          <a class="btn ghost" href={`${base}/division-2/builds/`}>Browse builds</a>
          <a class="btn ghost" href={`${base}/division-2/tools/`}>Open tools</a>
        </div>
      </div>

      <aside class="hero-command-panel">
        <a class="stat-item" href={`${base}/division-2/tools/`}>
          <span>{toolsCount}</span>
          <small>Tools Available</small>
        </a>

        <a class="stat-item" href={`${base}/division-2/videos/`}>
          <span>{videos.length}</span>
          <small>Videos Indexed</small>
        </a>

        <a class="stat-item" href={`${base}/division-2/creators/`}>
          <span>{creatorCount}</span>
          <small>Creators Listed</small>
        </a>

        <a class="stat-item stat-item--network" href={`${base}/division-2/`}>
          <span>Active</span>
          <small>Staging Review</small>
        </a>
      </aside>
    </section>
  </header>

  <section class="section shell mission-control-section" id="mission-control">
    <div class="section-head mission-control-head">
      <p class="eyebrow">Start Here</p>
      <h2>What are you looking for?</h2>
      <p>
        Choose the path that matches what you need right now. This keeps the
        site useful for newer players, returning agents, and experienced endgame players.
      </p>
    </div>

    <div class="mission-control-grid">
      <a class="mission-card mission-card-primary" href={`${base}/division-2/builds/`}>
        <span class="mission-kicker">Builds</span>
        <strong>Find a Build</strong>
        <small>Browse PvE, PvP, solo, Striker, and creator build videos.</small>
        <em>Open builds →</em>
      </a>

      <a class="mission-card" href={`${base}/division-2/tools/`}>
        <span class="mission-kicker">Tools</span>
        <strong>Research Gear</strong>
        <small>Look up exotics, named items, brand sets, gearsets, and talents.</small>
        <em>Open tools →</em>
      </a>

      <a class="mission-card" href={`${base}/division-2/`}>
        <span class="mission-kicker">Hub</span>
        <strong>Explore Division 2</strong>
        <small>Use the main Division 2 hub to reach every major section.</small>
        <em>Open hub →</em>
      </a>

      <a class="mission-card" href={`${base}/division-2/creators/`}>
        <span class="mission-kicker">Creators</span>
        <strong>Find Creator Videos</strong>
        <small>Discover videos and open them directly on the creator's YouTube channel.</small>
        <em>View creators →</em>
      </a>
    </div>

    <div class="mission-quick-strip">
      <a href={`${base}/division-2/`}>Division 2 Hub</a>
      <a href={`${base}/division-2/builds/striker/`}>Striker Builds</a>
      <a href={`${base}/division-2/tools/exotic-source-tracker/`}>Exotic Sources</a>
      <a href={`${base}/division-2/tools/named-items-database/`}>Named Items</a>
    </div>
  </section>

  <section class="section shell">
    <div class="section-head">
      <p class="eyebrow">Player Resources</p>
      <h2>Tools, builds, and reference databases</h2>
      <p>
        Use these sections to understand items, compare gear, find build ideas,
        and connect to creator videos without losing the original YouTube source.
      </p>
    </div>

    <div class="tools-grid">
      <a class="tool-card" href={`${base}/division-2/tools/exotic-source-tracker/`}>
        <span class="mission-kicker">Tool</span>
        <strong>Exotic Source Tracker</strong>
        <p>{exoticCount} exotic records with source and talent references.</p>
      </a>

      <a class="tool-card" href={`${base}/division-2/tools/named-items-database/`}>
        <span class="mission-kicker">Tool</span>
        <strong>Named Items Database</strong>
        <p>{namedCount} named item records for gear and weapon planning.</p>
      </a>

      <a class="tool-card" href={`${base}/division-2/videos/`}>
        <span class="mission-kicker">Videos</span>
        <strong>Creator Video Library</strong>
        <p>{videos.length} videos indexed for discovery with direct YouTube links.</p>
      </a>

      <a class="tool-card" href={`${base}/division-2/guides/`}>
        <span class="mission-kicker">Guides</span>
        <strong>Guide Routes</strong>
        <p>Helpful paths for farming, returning players, builds, and endgame activities.</p>
      </a>
    </div>
  </section>
</BaseLayout>
